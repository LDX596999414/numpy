import numpy as np

# x = np.random.rand(5, 5, 5)
x = np.arange(64).reshape(4, 4, 4)   # 前到后X 上到下Y 左到右Z 左前角 四维以上矩阵怎么理解？
a = x[0]   # 没有，说明就在第一维上操作
b = x[0, 1]
c = x[:]   # c
d = x
e = x[2:]
f = x[2:, 1:]
g = x[::, ::, ::-1]
h = x[[1, 2, 3], [0, 0, 0], [1, 1, 1]]  # ??????
i = x[[1, 2, 3], 3:]   # ?????????
j = x[1, ...]
k = x[x > 4]
print(h)


#

#  三维情况创建时后面2个参数可以看做是创建二维数组，
# 第1个参数看做创建的二维数组的个数，所以创建时输入的参数为2,3,2时，
# 就相当于创建了2个3行2列的二维数组
